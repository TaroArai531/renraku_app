<% provide(:title, "ユーザー管理") %>
<h1>All users</h1>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= user.name %>
    <% if current_user.admin? %>
      | <%= button_to "delete", admin_user_path(user), method: :delete %>
    <% end %>
  </li>
  <% end %>
</ul>
